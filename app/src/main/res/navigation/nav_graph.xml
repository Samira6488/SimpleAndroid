<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="ir.matiran.cryptocurrency.ListFragment"
        android:label="fragment_main"
        tools:layout="@layout/fragment_list" >
        <action
            android:id="@+id/action_mainFragment_to_fragment_second"
            app:destination="@id/fragment_second" />
    </fragment>
    <fragment
        android:id="@+id/fragment_second"
        android:name="ir.matiran.cryptocurrency.fragment_second"
        android:label="fragment_second"
        tools:layout="@layout/fragment_second" >
        <action android:id="@+id/action_fragment_second_to_mainFragment" />
    </fragment>
</navigation>